/*
Package input builds model input blocks from prompts, local files, and URLs.

It owns MIME/modality detection and the resource-loading logic that turns user
supplied paths into gai.Block values.
*/
package input
